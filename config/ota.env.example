# Copy to config/ota.env (ignored by git) and edit as needed.
# OTA_HOST is optional if you always pass HOST=... to make upload-ota, but
# setting it here saves typing.
OTA_HOST=192.168.1.129
OTA_PASSWORD=ChangeMe
